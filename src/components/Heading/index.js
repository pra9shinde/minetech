import React from 'react';
import './heading.css';

const index = ({ title }) => {
    return (
        <div className='heading'>
            <h1>{title}</h1>
        </div>
    );
};

export default index;
